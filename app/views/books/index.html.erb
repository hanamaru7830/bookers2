<%= render 'books/userinfo', book: @book, user: @user %>




<h1>Books</h1>
<% @books.each do |book| %>

<div>
  <%= link_to book_path(book.id) do %>


  <p><%= book.title %></p>
  <p><%= book.body %></p>
  <% end %>
</div>
<% end %>