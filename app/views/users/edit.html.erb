<h1>User info</h1>
<%= form_with model: @user, local:true do |f| %>
  <h4>name</h4>
  <%= f.text_field :name %>
  <h4>画像</h4>
  <%= f.attachment_field :profile_image %>
  <h4>Introduction</h4>
  <%= f.text_area :body %>
  <div class="actions">
    <%= f.submit 'Update User' %>
  </div>
<% end %>